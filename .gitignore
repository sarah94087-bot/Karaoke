# secrets
.env
.env.*
!.env.example
certs/

# local tooling state
.claude/

# python
.venv/
__pycache__/
*.pyc

# audio: input songs and every derived stem. These are large (the output tree is
# ~1GB) and fully reproducible from the source files, so they stay out of git.
input/
output/htdemucs/
output/remote/

# keep the measurements and reports, which are the actual results of phase 0
!output/*.md
!output/*.csv
!output/*.json

# transcripts are small and worth versioning, except the scratch audio
transcripts/_smoke.wav

# node
node_modules/
.next/
